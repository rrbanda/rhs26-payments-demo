apiVersion: v1
kind: Service
metadata:
  name: payment-ops
  namespace: rhs26-payments-demo
  labels:
    app.kubernetes.io/name: payment-ops
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: payment-ops
