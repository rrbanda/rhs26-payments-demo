# Secrets -- non-secret config lives in config.yaml

# LLM endpoint (LlamaStack / OpenAI-compatible)
LLAMASTACK_API_KEY=not-needed
LLM_API_BASE=https://llamastack-llamastack.apps.cluster-6crhb.6crhb.sandbox1011.opentlc.com/v1
LLM_MODEL_ID=openai/gemini/models/gemini-2.5-flash

# Neo4j (not used by this demo -- mock tools only)
NEO4J_PASSWORD=notused
